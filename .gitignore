# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
venv_gpu/
env/
ENV/
.venv

# Environment variables
.env
.env.local
.env.*.local

# Local deploy notes / helpers that must not contain real tokens (never commit)
configs/local/queue_notify.json
RAILWAY_TOKEN_UPDATE_STEPS.txt
RAILWAY_DEPLOY.md
EXACT_STEPS*.md
add_bot_token_railway.py
add_token_railway_cli.bat

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Stray Ultralytics downloads at repo root (use data/weights/ultralytics/ or runs/)
/*.pt

# Model files (too large for GitHub - use Git LFS if needed)
models/*.pt
!models/bes.pt
models/*.onnx
models/*.pth
models/*.weights
!models/.gitkeep

# Local tuning images (not for repo)
test_sunflower_tune.png

# Temporary files
*.tmp
*.log
*.temp
temp/
tmp/

# OS
Thumbs.db
.DS_Store

# Jupyter Notebook
.ipynb_checkpoints

# pytest
.pytest_cache/
.coverage
htmlcov/

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Cloned upstream training repos (configs/external/*.json stays tracked)
/external/

# Local datasets (keep raw data untracked)
data/*
!data/README.md
!data/manifest.json
!data/data.yaml.example
!data/splits/
!data/splits/**
!data/examples/
!data/examples/**
# Cached checkpoints (Ultralytics + external); see scripts/check_weights_cache.py
# data/weights/ is under data/* above

# Generated artifacts (README placeholders only; metrics/JSON live on disk or CI)
reports/*
!reports/.gitkeep
!reports/README.md
!reports/*/
!reports/**/README.md
reports/**/*
!reports/**/README.md
figures/*
!figures/.gitkeep
!figures/README.md

# Structured pipeline outputs (local)
outputs/*
!outputs/.gitkeep
!outputs/README.md

# Ultralytics runs output
runs/

# Local venv used for requirements checks
.venv_reqcheck/

