python-telegram-bot>=22.5
sahi>=0.11.18
opencv-python-headless>=4.8.0
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
scipy>=1.10.0
pillow>=10.0.0
python-dotenv>=1.0.0
aiohttp>=3.9.0

#
# Optional / heavyweight dependencies (NOT installed in CI or default bootstrap).
# Install via scripts/bootstrap_env.py flags (see docs/RESEARCH_AND_OPS.md).
#
# External DETR stack (DEIM, D-FINE, RT-DETRv2) — bootstrap --with-external-detr
# faster-coco-eval
# calflops
# transformers
# loguru
# gdown

# YOLO-NAS backend (SuperGradients) — bootstrap --with-super-gradients
# super-gradients
