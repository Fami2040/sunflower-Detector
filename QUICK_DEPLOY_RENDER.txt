═══════════════════════════════════════════════════════════════
  QUICK DEPLOY TO RENDER.COM - COPY & PASTE INSTRUCTIONS
═══════════════════════════════════════════════════════════════

STEP 1: Go to Render.com
   → https://render.com
   → Click "Get Started for Free"
   → Sign up with GitHub (recommended)

STEP 2: Create Background Worker
   → Click "New +" button (top right)
   → Select "Background Worker"

STEP 3: Connect Your Repo
   → Select "sunflower-Detector" repository
   → Click "Connect"

STEP 4: Configure Settings
   
   Name: sunflower-bot
   Region: Oregon (US West) [or closest to you]
   Branch: main
   
   Environment: Python 3
   Build Command: pip install -r requirements.txt
   Start Command: python telegram_bot.py
   
   Environment Variables:
   → Click "Add Environment Variable"
   → Key: BOT_TOKEN
   → Value: 8527984904:AAEZSOQ25RMpyRcsYEy1TWxiYeEbZfzDqHY
   
   (Replace with your actual bot token!)

STEP 5: Deploy
   → Click "Create Background Worker"
   → Wait for build to complete (5-10 minutes)
   → Check logs for "🤖 Bot is starting..."

STEP 6: Test
   → Go to Telegram
   → Send /start to your bot
   → If it responds, you're done! ✅

═══════════════════════════════════════════════════════════════
  TROUBLESHOOTING
═══════════════════════════════════════════════════════════════

If bot doesn't respond:
   → Check Render dashboard logs
   → Verify BOT_TOKEN is correct
   → Make sure models are in repository

If build fails:
   → Check logs for error messages
   → Verify requirements.txt is correct

═══════════════════════════════════════════════════════════════




