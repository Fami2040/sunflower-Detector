# Figures (generated)

This directory is the default output location for regenerated manuscript figures.

- Prefer deterministic generation via `scripts/make_figures.py`.
- Do not commit large binary figure outputs by default.

