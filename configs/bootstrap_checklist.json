{
  "schema_version": "bootstrap_checklist.v1",
  "heading": "Post-install checklist:",
  "commands": [
    "mamba run -n {env} python scripts/check_gpu.py",
    "mamba run -n {env} python scripts/check_gpu.py --verify-deps",
    "export HARCHOC_EXPORT_DEVICE=cpu  # optional: preds export when GPU OOM @ 1280",
    "python scripts/check_weights_cache.py --strict --out reports/weights_cache.json",
    "export DATASET_NAME=sunflower-cvat-2500  # or DATASET_ROOT=... && mamba run -n {env} python scripts/validate_splits.py --require-test"
  ],
  "when": {
    "with_super_gradients": [
      "mamba run -n {env} python -c \"import super_gradients; print('super_gradients', super_gradients.__version__)\""
    ]
  },
  "footer": "See docs/EXPERIMENTS.md (weights cache + dataset resolution)."
}
