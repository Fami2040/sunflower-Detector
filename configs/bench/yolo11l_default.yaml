include: _defaults.yaml
name: bench_yolo11l_default
backend: ultralytics
model: yolo11l.pt
groups: yolo11_scales, sota_2026, zoo_scale
infer:
  max_det: 3000
  tiling: "none"
notes: >
  YOLO11l zoo entry @ 1280. Matrix train via train_bench_yolo11l.json.
