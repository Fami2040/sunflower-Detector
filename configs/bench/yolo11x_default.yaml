include: _defaults.yaml
name: bench_yolo11x_default
backend: ultralytics
model: yolo11x.pt
groups: yolo11_scales, sota_2026, zoo_scale
infer:
  max_det: 3000
  tiling: "none"
notes: >
  YOLO11x zoo entry @ 1280. Matrix train via train_bench_yolo11x.json.
