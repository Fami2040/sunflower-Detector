include: _defaults.yaml
name: bench_yolov10s_default
backend: ultralytics
model: yolov10s.pt
groups: yolov10_scales, sota_2026, zoo_scale
infer:
  max_det: 3000
  tiling: "none"
notes: >
  YOLOv10s zoo entry @ 1280. Matrix train via train_bench_yolov10s.json.
