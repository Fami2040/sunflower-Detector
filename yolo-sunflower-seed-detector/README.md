# Legacy training shim

Training and evaluation live in [`scripts/train.py`](../scripts/train.py) and [`scripts/eval.py`](../scripts/eval.py). Dataset and weights: [`docs/ORIGIN_MAIN_AND_DATASET.md`](../docs/ORIGIN_MAIN_AND_DATASET.md).

`training.py` in this folder forwards to the canonical train entrypoint.
